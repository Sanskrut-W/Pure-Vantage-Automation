export const bannerLocators = {
    // Relying on accessible properties or generic CSS fallbacks
    buttonCreateBanner: 'button[aria-label="Create Banner"]', // As requested via aria-label
    dropdownRegion: '.p-dropdown',                            // CSS class per requirements
    inputSearch: 'input[placeholder="Search"]',               // Placeholder per requirements
    
    // Additional potentially helpful generic locators
    pageTitle: 'h1, h2, .page-title'
};
